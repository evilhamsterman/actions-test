name: Github Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Set env
        run: echo ANSIBLE_LOG_PATH=${{ runner.temp }}/logs/ansible.log >> $GITHUB_ENV
      - name: check env
        run: echo $ANSIBLE_LOG_PATH
